# Core Dependencies
PyQt5==5.15.2
cryptography==3.4.7
bcrypt==4.1.2
requests==2.31.0  # Required for update system and GitHub API

# Windows-specific Dependencies
pywin32==306  # Required for Windows-specific functionality

# Optional Security Dependencies
pyarmor==8.0.0  # Optional: For code obfuscation (install with: pip install pyarmor)

# Required Files (must be in the same directory as the main script):
# - Test.py (main application file)
# - Insert_data.ui (UI definition file)
# - resources.qrc (resource file)
# - resources_rc.py (compiled resources)
# - users.json (user database)
# - data.encrypted (encrypted data file)
# - resources/ (directory containing all resource files)
#   - images/ (directory containing all image assets)
#     - Pharmacist.jpeg

# Installation Instructions:
# 1. Install Python 3.8 or later
# 2. Run: pip install -r requirements_windows.txt
# 3. Run the application with: python Test.py

# Note: The application requires read/write permissions in its directory
# for the data.encrypted and users.json files.
